import React from 'react'
import { Typography } from '@material-ui/core'
import { Box } from '@material-ui/core'

export default () => {

    return (
        <Box mt={3}>
            <Typography variant="h5">
                Sign in with Steam to see your account information
            </Typography>
        </Box>
    )
}